---
declared-services:
  my-conversation-service:
    label: conversation
    plan: free
applications:
- name: Conv_Siva
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - Conversation-1
  env:
    NPM_CONFIG_PRODUCTION: false
